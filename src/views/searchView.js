// src/views/searchView.js
const appEl = document.getElementById("app");

export function renderSearch() {
  appEl.innerHTML = `
    <header class="page-header">
      <h1>Tìm kiếm nhà hàng</h1>
    </header>
    <main>
      <p>Trang search (đang làm)...</p>
    </main>
  `;
}
